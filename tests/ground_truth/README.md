# Ground Truth

`staffa_test_1_expected.json` contains the reference data extracted from the AutoForm PDF for STAFFA TEST 1.

`tests/test_files/STAFFA TEST 1.stp` is the real STEP file analyzed by the API. The STEP file is used as the CAD input fixture, while the AutoForm PDF data is the comparison ground truth.
